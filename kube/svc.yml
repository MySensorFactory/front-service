apiVersion: v1
kind: Service
metadata:
  name: front-service-service
spec:
  selector:
    app: front-service-deployment
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: 8080